# First-party analytics store (api/track.ts). Turso in production; locally,
# omit both and events go to .data/landing-events.db via scripts/dev-api.ts.
TURSO_DATABASE_URL="libsql://sdg-landing-events-<org>.turso.io"
TURSO_AUTH_TOKEN=""
